name: Manage Hardware

on:
  push:
    branches: [main]
  
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Test
        run: |
          echo "hello"
          ls
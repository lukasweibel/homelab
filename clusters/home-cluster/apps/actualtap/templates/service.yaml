apiVersion: v1
kind: Service
metadata:
  name: {{ include "actualtap.fullname" . }}
  labels:
    {{- include "actualtap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "actualtap.selectorLabels" . | nindent 4 }}
